<% if notice %>
<p id="notice"> <%= notice %> </p>
<% end %>

<h2>Your Cart</h2>
<table>
   <%= render @cart %>
</table>

<%= button_to 'Empty cart', @cart, method: :delete,
data: {confirm: 'Are you sure?'}%>
